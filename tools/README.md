# MCP Tools

Development-only tools using the Model Context Protocol (MCP).

## Available MCP Servers

### e2e-test-generator
Generates Playwright e2e tests from user stories.

```bash
cd e2e-test-generator
pnpm install
pnpm start
```

**Note:** These tools are NOT part of the Next.js app build and should be used independently for development workflows.
