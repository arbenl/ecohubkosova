// TypeScript still tries to load the deprecated stub package name
// `testing-library__jest-dom`, so point it to the package that actually
// ships the definitions.
export * from "@testing-library/jest-dom"
